{
  "name": "@ducklings/example-cloudflare-worker-ducklake",
  "version": "1.5.3",
  "private": true,
  "type": "module",
  "scripts": {
    "check-deps": "test -f ../ducklings-workers-ducklake/dist/index.js || (echo 'Error: @ducklings/workers-ducklake has not been built yet.' && echo 'Run the following from the repo root first:' && echo '' && echo '  make duckdb-workers-ducklake      # compile DuckLake WASM with Asyncify' && echo '  make typescript-workers-ducklake   # build TypeScript package' && exit 1)",
    "predev": "pnpm run check-deps",
    "prebuild": "pnpm run check-deps",
    "build": "vite build",
    "dev": "pnpm run build && wrangler dev",
    "deploy": "pnpm run build && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ducklings/workers-ducklake": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.26.1",
    "@cloudflare/workers-types": "^4.20260310.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "wrangler": "^4.71.0"
  }
}
